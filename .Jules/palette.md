## 2025-05-18 - [Visibility of Constraints]
**Learning:** Users can feel frustrated when they hit invisible limits (like character counts) without warning. Showing the limit upfront (e.g., "0/100") transforms a potential error into a guided interaction.
**Action:** Always expose input constraints visually near the input field, using `supportingText` in Material 3 text fields where possible.
